import chalk from 'chalk';
export function printFooter(): void {
  // Footer with call to action
  console.log(chalk.bgBlue.white.bold(' ⬇ Happy Coding ⬇ '));
  console.log(chalk.greenBright.bold('🌍 Keep pushing boundaries! 🚀'));
}